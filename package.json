{
  "private": true,
  "scripts": {
    "build": "cd ./client && npm run build",
    "start:client": "cd ./client && npm start",
    "start:server": "cd ./backend && npm start",
    "deploy": "cd ./client && npm i && npm run build"
  }
}
